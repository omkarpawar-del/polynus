'use client'; import { createClient } from '@/lib/supabase/client'; import { useEffect,useState } from 'react';
export default function Connect(){ const [user,setUser]=useState<string|null>(null); const [email,setEmail]=useState(''); useEffect(()=>{createClient().auth.getUser().then(({data})=>setUser(data.user?.email??null))},[]); const login=async()=>{await createClient().auth.signInWithOAuth({provider:'google',options:{redirectTo:`${location.origin}/auth/callback?next=/connect`}})}; const demo=async(provider:string)=>{await fetch('/api/connections',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({provider})}); location.href='/'}; return <main className="mx-auto max-w-2xl px-5 py-14"><a href="/" className="text-sm text-violet-300">← Dashboard</a><p className="mt-12 text-xs font-semibold uppercase tracking-[.25em] text-violet-300">Connections</p><h1 className="mt-2 text-4xl font-semibold">Bring your AI work together.</h1><p className="mt-4 text-slate-400">Sign in to create your private Polynus workspace, then import approved conversation exports or use the demo sync.</p>{!user?<button onClick={login} className="mt-8 rounded-xl bg-white px-5 py-3 font-medium text-slate-950">Sign in with Google</button>:<><p className="mt-6 text-sm text-emerald-300">Signed in as {user}</p><div className="mt-8 grid gap-4 sm:grid-cols-2"><section className="panel p-6"><h2 className="text-xl font-medium">ChatGPT</h2><p className="mt-2 text-sm text-slate-400">OpenAI does not provide consumer ChatGPT-history OAuth. Import a ChatGPT data export to analyze it.</p><button onClick={()=>demo('openai')} className="mt-5 rounded-lg bg-violet-500 px-4 py-2 text-sm">Connect demo data</button></section><section className="panel p-6"><h2 className="text-xl font-medium">Google Gemini</h2><p className="mt-2 text-sm text-slate-400">Gemini API keys do not include Gemini Apps chat history. Import an export to analyze it.</p><button onClick={()=>demo('gemini')} className="mt-5 rounded-lg bg-emerald-500 px-4 py-2 text-sm">Connect demo data</button></section></div></>}<p className="mt-10 text-xs text-slate-500">Your Google sign-in uses Supabase Auth. Provider credentials are encrypted at rest when supported OAuth scopes are available.</p></main> }
